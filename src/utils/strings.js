// Here will be all the strings methods used in the app

// Capitalize the first letter of a string
export const capitalize = (string) => {
  return string.charAt(0).toUpperCase() + string.slice(1);
};
